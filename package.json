{
  "dependencies": {
    "buffer": "^6.0.3"
  },
  "scripts": {
    "start-api": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && flask run --no-debugger",
    "start-ui": "cd frontend && npm install && npm start"
  }
}